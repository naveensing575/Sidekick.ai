// src/app/viewport.ts
import type { Viewport } from 'next'

const viewport: Viewport = {
  themeColor: '#000000',
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
}

export default viewport
